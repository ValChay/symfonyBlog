{% extends 'base.html.twig' %}

{% block title %} {{ 'article.edit.title' | trans }}{% endblock %}

{% block body %}
    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <input type="submit" value="{{ 'article.edit.submit' | trans }}">
    {{ form_end(edit_form) }}
{% endblock %}


{% extends 'base.html.twig' %}

{% block title %} {{  'article.create.title' | trans }}{% endblock %}

{% block body %}


    {{ form_start(myform) }}
    {{ form_widget(myform) }}
    <input class="primary" type="submit" value="{{ 'article.create.submit'| trans }}">
    {{ form_end(myform) }}
{% endblock %}
